# render.yaml ejemplo
services:
  - type: web
    name: barberia-system
    env: python
    pythonVersion: 3.11
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn barberia_system.wsgi:application --bind 0.0.0.0:$PORT